# Resume LaTeX Source

Edit `resume.tex` in this folder.

To rebuild the public resume at `https://snehithn.com/resume.pdf`, run from the repository root:

```sh
./build-resume.sh
```

The script compiles in a temporary directory and writes the only committed PDF to `../resume.pdf`.
